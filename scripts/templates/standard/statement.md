# {{PROBLEM_TITLE}}

This scaffold statement is intentionally complete and runnable: read one integer `n` and print the same integer.

## Input

One line containing an integer `n`.

## Output

Output one line containing the same integer `n`.

## Sample

```input1
5
```

```output1
5
```

## Explanation

The sample input is `5`, so the correct output is also `5`.
