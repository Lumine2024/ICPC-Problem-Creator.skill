# {{PROBLEM_TITLE}} Tutorial

## Summary

This scaffold problem asks contestants to read one integer `n` and print the same value.

## Solution

The intended solution is direct simulation:

1. Read the integer.
2. Print it unchanged.

No corner case requires special handling beyond respecting the input format.

## Correctness

The output is defined to be exactly the input integer. Returning `n` therefore matches the specification for every legal test case.

## Complexity

- Time: `O(1)`
- Space: `O(1)`

## Notes

When this scaffold is replaced by a real contest problem, keep the tutorial complete and remove any text that no longer matches the final statement.
