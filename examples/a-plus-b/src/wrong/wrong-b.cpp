#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long a = 0, b = 0;
    cin >> a >> b;
    cout << llabs(a) + llabs(b) << '\n';
    return 0;
}
